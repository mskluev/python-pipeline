[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "infer_core"
version = "0.1.0"
description = "Shared internal Python code"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.0.0"
]
