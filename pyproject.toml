[project]
name = "python-pipeline"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[tool.uv.workspace]
members = [
    "libraries/*",
    "services/*"
]

[dependency-groups]
dev = [
    "boto3>=1.42.93",
    "numpy>=2.4.4",
    "tritonclient[http]>=2.67.0",
]
